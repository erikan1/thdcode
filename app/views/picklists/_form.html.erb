<%= form_with(model: picklist, local: true) do |form| %>
  <% if picklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picklist.errors.count, "error") %> prohibited this picklist from being saved:</h2>

      <ul>
      <% picklist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
